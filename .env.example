# ---- GCP / CXAS ----
GCP_PROJECT_ID=decent-courage-233916
GCP_PROJECT_NUMBER=1078946815141
GCP_LOCATION=us
CXAS_APP_NAME=
CXAS_AGENT_ID=
CXAS_DEPLOYMENT_ID=

# Client-side pacing for the CES per-minute "RunSession LLM tokens" quota.
# Lower this if you see 429s; raise it (or request a quota increase) for speed.
CXAS_REQUESTS_PER_MINUTE=6

# Optional: service account JSON (otherwise uses Application Default Credentials)
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# ---- Demo mode ----
DEMO_MODE=scripted   # "scripted" or "live"

# ---- Frontend ----
VITE_API_URL=http://localhost:8000
VITE_DEMO_MODE=scripted
